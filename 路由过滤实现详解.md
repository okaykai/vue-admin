# Vue Admin Main 路由过滤实现详解

## 概述

路由过滤是权限管理系统的核心功能，用于根据用户权限动态显示和加载路由。本文档详细解释前端实现机制和后端数据需求。

## 前端路由过滤实现

### 1. 核心过滤函数

当前项目中已经实现了路由过滤函数，但被注释掉了：

```typescript
// 位置：src/store/modules/user.ts 第27-35行
function filterAsyncRoute(asyncRoute: any, routes: any) {
  return asyncRoute.filter((item: any) => {
    // 检查当前路由的name是否在用户权限routes数组中
    if (routes.includes(item.name)) {
      // 如果有子路由，递归过滤子路由
      if (item.children && item.children.length > 0) {
        item.children = filterAsyncRoute(item.children, routes)
      }
      return true // 保留此路由
    }
    // 用户无权限，过滤掉此路由
    return false
  })
}
```

### 2. 过滤机制详解

#### 2.1 过滤原理

- **匹配规则**：通过路由的 `name` 字段与用户权限的 `routes` 数组进行匹配
- **递归过滤**：对多级嵌套路由进行递归过滤
- **动态生成**：根据过滤结果动态生成用户可访问的路由

#### 2.2 完整实现流程

```typescript
async userInfo() {
  let res: userInfoResponseData = await reqUserInfo()

  if (res.code === 200) {
    this.username = res.data.name as string
    this.avatar = res.data.avatar as string
    this.buttons = res.data.buttons  // 按钮权限

    // 🔥 启用路由过滤的代码
    let userAsyncRoute = filterAsyncRoute(
      cloneDeep(asyncRoute),    // 深拷贝所有异步路由
      res.data.routes           // 用户权限路由数组
    )

    // 组合最终的菜单路由
    this.menuRoutes = [...constantRoute, ...userAsyncRoute, anyRoute]

    // 记录动态添加的路由（用于登出时移除）
    dynamicRoutes = [...userAsyncRoute, anyRoute]

    // 动态添加路由到Vue Router
    dynamicRoutes.forEach((route) => {
      router.addRoute(route)
    })

    return 'ok'
  }
}
```

### 3. 路由配置要求

#### 3.1 异步路由配置

每个需要权限控制的路由必须有唯一的 `name` 字段：

```typescript
// src/router/routes.ts
export const asyncRoute = [
  {
    path: '/acl',
    component: () => import('@/layout/index.vue'),
    name: 'Acl', // 🔑 关键：路由名称，用于权限匹配
    meta: {
      title: '权限管理',
      hidden: false,
      icon: 'Lock',
    },
    redirect: '/acl/user',
    children: [
      {
        path: '/acl/user',
        component: () => import('@/views/acl/user/index.vue'),
        name: 'User', // 🔑 子路由名称
        meta: {
          title: '用户管理',
          hidden: false,
          icon: 'User',
        },
      },
      {
        path: '/acl/role',
        component: () => import('@/views/acl/role/index.vue'),
        name: 'Role', // 🔑 子路由名称
        meta: {
          title: '角色管理',
          hidden: false,
          icon: 'Avatar',
        },
      },
    ],
  },
  {
    path: '/product',
    component: () => import('@/layout/index.vue'),
    name: 'Product', // 🔑 另一个模块的路由名称
    meta: {
      title: '商品管理',
      hidden: false,
      icon: 'Goods',
    },
    children: [
      // ... 子路由配置
    ],
  },
]
```

## 后端数据需求

### 1. 用户信息接口返回格式

后端 `/admin/acl/index/info` 接口需要返回以下数据结构：

```typescript
// TypeScript 类型定义
export interface userInfoResponseData extends ResponseData {
  data: {
    routes: string[] // 🔑 用户有权限访问的路由名称数组
    buttons: string[] // 🔑 用户有权限访问的按钮标识数组
    roles: string[] // 用户角色数组
    name: string // 用户名
    avatar: string // 用户头像URL
  }
}
```

### 2. 具体数据示例

#### 2.1 管理员用户（全部权限）

```json
{
  "code": 200,
  "message": "success",
  "data": {
    "name": "admin",
    "avatar": "https://example.com/avatar/admin.jpg",
    "roles": ["admin"],
    "routes": [
      "Acl", // 权限管理模块
      "User", // 用户管理
      "Role", // 角色管理
      "Permission", // 菜单管理
      "Product", // 商品管理模块
      "Trademark", // 品牌管理
      "Attr", // 属性管理
      "Spu", // SPU管理
      "Sku" // SKU管理
    ],
    "buttons": [
      "User:Add", // 用户添加按钮
      "User:Edit", // 用户编辑按钮
      "User:Delete", // 用户删除按钮
      "Role:Add", // 角色添加按钮
      "Role:Edit", // 角色编辑按钮
      "Role:Delete", // 角色删除按钮
      "Product:Add", // 商品添加按钮
      "Product:Edit", // 商品编辑按钮
      "Product:Delete" // 商品删除按钮
    ]
  }
}
```

#### 2.2 普通用户（部分权限）

```json
{
  "code": 200,
  "message": "success",
  "data": {
    "name": "operator",
    "avatar": "https://example.com/avatar/operator.jpg",
    "roles": ["operator"],
    "routes": [
      "Product", // 只有商品管理权限
      "Trademark", // 品牌管理
      "Attr" // 属性管理（无SPU、SKU权限）
    ],
    "buttons": [
      "Product:Add", // 只有添加和编辑权限
      "Product:Edit" // 无删除权限
    ]
  }
}
```

#### 2.3 只读用户（查看权限）

```json
{
  "code": 200,
  "message": "success",
  "data": {
    "name": "viewer",
    "avatar": "https://example.com/avatar/viewer.jpg",
    "roles": ["viewer"],
    "routes": [
      "Product", // 只能查看商品
      "Trademark" // 只能查看品牌
    ],
    "buttons": [] // 无任何操作按钮权限
  }
}
```

## 权限匹配规则

### 1. 路由权限匹配

```typescript
// 前端匹配逻辑
function hasRoutePermission(routeName: string, userRoutes: string[]): boolean {
  return userRoutes.includes(routeName)
}

// 过滤示例
const asyncRoute = [
  { name: 'Acl', children: [...] },      // 权限管理
  { name: 'Product', children: [...] }   // 商品管理
]

const userRoutes = ['Product', 'Trademark']  // 用户只有商品相关权限

// 过滤结果：只保留 Product 模块
const filteredRoutes = filterAsyncRoute(asyncRoute, userRoutes)
```

### 2. 按钮权限匹配

```vue
<!-- 模板中使用 v-has 指令 -->
<template>
  <el-button v-has="'User:Add'" @click="addUser">添加用户</el-button>
  <el-button v-has="'User:Edit'" @click="editUser">编辑用户</el-button>
  <el-button v-has="'User:Delete'" @click="deleteUser">删除用户</el-button>
</template>
```

```typescript
// 指令实现逻辑
app.directive('has', {
  mounted(el: any, binding: any) {
    const buttonCode = binding.value // 如 'User:Add'
    const hasPermission = userStore.buttons.includes(buttonCode)

    if (!hasPermission) {
      el.parentNode.removeChild(el) // 移除按钮
    }
  },
})
```

## 完整启用步骤

### 1. 前端代码修改

修改 `src/store/modules/user.ts` 中的 `userInfo` 方法：

```typescript
async userInfo() {
  let res: userInfoResponseData = await reqUserInfo()

  if (res.code === 200) {
    this.username = res.data.name as string
    this.avatar = res.data.avatar as string
    this.buttons = res.data.buttons || []  // 设置按钮权限

    // ✅ 启用动态路由过滤
    let userAsyncRoute = filterAsyncRoute(
      cloneDeep(asyncRoute),
      res.data.routes || []  // 用户路由权限
    )

    this.menuRoutes = [...constantRoute, ...userAsyncRoute, anyRoute]
    dynamicRoutes = [...userAsyncRoute, anyRoute]

    dynamicRoutes.forEach((route) => {
      router.addRoute(route)
    })

    return 'ok'
  } else {
    return Promise.reject(new Error(res.message))
  }
}
```

### 2. 后端接口要求

确保 `/admin/acl/index/info` 接口返回：

- `data.routes`：用户有权限的路由名称数组
- `data.buttons`：用户有权限的按钮标识数组

### 3. 权限配置建议

#### 3.1 路由权限层级规则

```
层级1：模块权限    - 如 'Acl'、'Product'
层级2：页面权限    - 如 'User'、'Role'、'Trademark'
层级3：功能权限    - 如 'User:List'、'User:Detail'
```

#### 3.2 按钮权限命名规范

```
格式：{模块}:{操作}
示例：
- User:Add       用户添加
- User:Edit      用户编辑
- User:Delete    用户删除
- Role:Assign    角色分配
- Product:Export 商品导出
```

## 测试验证

### 1. 路由过滤测试

1. 使用不同权限的用户登录
2. 检查侧边栏菜单是否正确显示
3. 直接访问无权限路由，应跳转 404

### 2. 按钮权限测试

1. 检查页面按钮是否根据权限显示/隐藏
2. 验证 v-has 指令是否正确工作

### 3. 权限更新测试

1. 修改用户权限后重新登录
2. 验证菜单和按钮权限是否实时更新

## 常见问题解决

### Q1: 菜单显示异常

**原因**：路由 name 与后端返回的 routes 不匹配
**解决**：检查路由配置的 name 字段与后端数据一致性

### Q2: 按钮权限不生效

**原因**：buttons 数组为空或 v-has 指令值不匹配
**解决**：确保后端返回 buttons 数据，检查按钮权限标识

### Q3: 动态路由 404

**原因**：路由过滤后某些路由被移除
**解决**：确保用户有访问该路由的权限，或处理无权限时的跳转逻辑

## 扩展功能

### 1. 路由权限缓存

```typescript
// 缓存用户权限，避免频繁请求
const PERMISSION_CACHE_KEY = 'user_permissions'

// 设置缓存
localStorage.setItem(
  PERMISSION_CACHE_KEY,
  JSON.stringify({
    routes: res.data.routes,
    buttons: res.data.buttons,
    timestamp: Date.now(),
  }),
)
```

### 2. 权限实时更新

```typescript
// WebSocket监听权限变更
socket.on('permission-updated', (newPermissions) => {
  // 更新权限并重新过滤路由
  userStore.updatePermissions(newPermissions)
})
```

### 3. 页面级权限守卫

```typescript
// 在路由元信息中添加权限要求
{
  path: '/sensitive-page',
  meta: {
    requiresAuth: true,
    permissions: ['Admin:HighLevel']
  }
}
```

通过以上实现，可以建立一套完整的动态路由权限管理系统，实现精细化的权限控制。
